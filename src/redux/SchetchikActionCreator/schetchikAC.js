import { SCHETCHIK, SCHETCHIK_MINUS, SCHETCHIK_PLUS } from "../types";



export const schetchikAC = () => ({ type: SCHETCHIK })

export const schetchikPlusAC = () => ({ type: SCHETCHIK_PLUS })

export const schetchikMinusAC = () => ({ type: SCHETCHIK_MINUS })

// export const schetchikHyiAC = () => ({ type: SCHETCHIK })

